---
- name: Installing Python Package
  hosts: all
  become: true
  gather_facts: true

  tasks:
    - name: apt update
      apt:
        update_cache: true
    - name: install package
      apt:
        name:
          - python3-lxml
          - python3-libvirt
