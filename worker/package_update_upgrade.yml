---
- name: apt Update Upgrade
  hosts: all # when executing playbook, you can limit hosts with "--limit $children"
  become: true
  gather_facts: true

  tasks:
    - name: apt update
      apt:
        update_cache: true
    - name: apt upgrade
      apt:
        upgrade: true
